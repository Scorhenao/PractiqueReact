import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20,
    },
    title: {
        fontSize: 24,
        fontWeight: 'bold',
        textAlign: 'center',
        marginBottom: 20,
    },
    description: {
        fontSize: 16,
        textAlign: 'center',
        marginBottom: 20,
    },
    featureTitle: {
        fontSize: 18,
        fontWeight: 'bold',
        marginBottom: 20,
        textAlign: 'center',
    },
    tutorialStep: {
        marginBottom: 30,
        alignItems: 'center',
    },
    image: {
        width: 300,
        height: 500,
        resizeMode: 'contain',
        marginBottom: 10,
    },
    stepText: {
        fontSize: 16,
        textAlign: 'center',
    },
});
